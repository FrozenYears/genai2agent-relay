# Action 封套两次小修记录

## 范围

两次修改均位于独立的 `genai2agent-relay` 仓库，不改变客户端协议或工具执行端。保留严格 JSON、工具白名单、参数 Schema 校验及现有重试次数；不自动补括号，不新增日志功能、配置项或依赖。

## 第一次：说明文字被误判为工具调用

原因：正文用反引号展示 `@@ACTION@@{...}@@END_ACTION@@` 时，旧解析器仍将标记当成真实调用，随后因封套不在结尾而拒绝整段回复。

修改：跳过已闭合的单行反引号引用及配对的反引号/波浪线代码围栏（围栏长度一致）。找到引用外的真实调用起点后，不扫描参数中的 Markdown，避免误伤合法参数。

| 情况 | 修改前 | 修改后 |
| --- | --- | --- |
| 正文引用 action 示例 | 可能返回 502 | 返回正文，不执行工具 |
| 引用后跟合法真实调用 | 需要兼容 | 仍识别真实调用 |
| 参数含反引号或 action 标记 | 应保留参数 | 保持参数不变 |
| 真实调用未闭合或 JSON 非法 | 拒绝 | 仍拒绝 |

边界：这不是完整 Markdown 解析器；未闭合或不支持的引用仍按严格规则处理。实际调用不得包进 Markdown；自动工具选择模式下，引用内调用作为文本返回，不能执行。

## 第二次：JSON 重试缺少具体纠错信息

原因：一个双调用封套在第一个 `parameters` 后少了关闭调用对象的 `}`。旧代码已有一次自动重试，但丢弃 JSON 解析原因，只笼统要求重新序列化。

修改：保留 `JSONDecodeError` 异常原因，仅在 JSON 语法错误时将原因、封套内 JSON 的行列号加入已有重试提示，要求完整重发而不是补丁。失败回复原本就在重试历史中，无需再次复制参数。回溯封套起点时更新诊断，避免选中参数内嵌标记对应的错误位置。

| 情况 | 修改前 | 修改后 |
| --- | --- | --- |
| JSON 语法错误后的重试 | 泛化提示 | 提示具体原因及行列号 |
| 重试修正成功 | 交付工具调用 | 不变 |
| 重试仍失败 | 502 / api_error | 不变，客户端错误文本不变 |
| 非 JSON 语法错误 | 原有重试提示 | 不变 |
| 日志、次数、超时、配置 | 原有行为 | 不变 |

该改动提高纠错信息质量，不保证模型第二次一定修正，也不解决平台网页未显示回复的原因。未新增逐次诊断日志。

## 验证

- 第一次：23 项中继测试通过；用户提供的完整说明回复经 Messages 路由返回 HTTP 200，正文原样保留，无需重试。
- 第二次：24 项中继测试通过，包含参数内嵌标记时的定位、重试成功和持续失败边界。
- 使用用户提供的缺括号样本进行 Messages 路由验证：重试提示包含第 1 行、第 293 列；模拟第二次返回修正封套时 HTTP 200，两个调用的参数保持不变；第二次仍错误时 HTTP 502，错误文本保持不变。
- 验证使用合成上游回复，未执行样本命令、未请求真实模型；尚不能量化真实模型纠错成功率。验证脚本在内存运行，无临时文件。

## 生效与回滚

修改源码后需重启中继；本轮未重启正在运行的服务。

中继仓库的第一次修复提交为 `64f6a80`，之前基线为 `c101d1a`。第二次修改与本文独立提交，提交标题为 `fix: add JSON error location to serialization retry`。

在中继仓库运行 `git log --oneline` 找到第二次提交，然后执行 `git revert <第二次提交号>` 可仅撤销本次修改。若两次都要撤销，先撤销第二次，再执行 `git revert 64f6a80`。最后重启中继。不使用 `reset --hard`，不删除凭据或环境。
